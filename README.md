# Zicuro
Take the pull from 'C# codes branch in your local machine
Run the remote server using node main.js
Run your .net proj using VSC 
Post  successful recieving of packets from remote server one can observe on json file created in the  bin folder
